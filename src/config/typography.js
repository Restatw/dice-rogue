// 字體大小設定檔 — 所有場景的 pixelText size 參數統一在此管理。
// 命名原則：語意角色優先，避免用像素數字命名。
// 修改此檔案即可全域調整對應層級的字體大小。
// 畫布尺寸：1080×1920（原 450×975，字體比例 ×2.4）

export const FS = {
  // ── 主標題 ────────────────────────────────────────────────
  gameTitle:    154,  // 主選單遊戲名稱「骰途」
  resultTitle:  115,  // 結果畫面 通關／冒險結束

  // ── 場景標題 ──────────────────────────────────────────────
  sceneTitle:    96,  // 場景大標，如「選擇難度」
  winMsg:        86,  // 戰鬥結束 大字提示（全滅等）
  victoryGold:   67,  // 勝利金幣訊息
  sectionHead:   53,  // 面板／畫面小節標題（隊伍組成、商店等）

  // ── 面板 / 按鈕 ───────────────────────────────────────────
  panelTitle:    48,  // 地圖場景面板標題（休息、商店）
  btnDefault:    43,  // 按鈕預設（widgets button）
  mapTitle:      43,  // 大地圖頂部標題
  rulesTitle:    43,  // 規則面板標題
  itemName:      43,  // 戰利品道具名稱

  // ── 一般內文 ──────────────────────────────────────────────
  body:          38,  // 主選單副標、選角卡姓名
  toastMsg:      34,  // 浮動提示、購買訊息、說明文字
  actorLabel:    31,  // 戰鬥行動者提示、背包/商店標籤
  combatInfo:    29,  // 戰鬥 infoTxt、選角職業、規則面板等
  cardName:      29,  // 英雄卡名字（戰鬥場景）

  // ── 標籤 / 數值 ───────────────────────────────────────────
  nodeLabel:     26,  // 地圖節點標籤、商店分隔線
  roleName:      29,  // 選角角色職業說明
  abilityCode:   26,  // 選角能力編碼
  mapParty:      26,  // 地圖頂部隊伍一覽

  // ── 小標籤 ────────────────────────────────────────────────
  elemName:      24,  // 元素名稱（敵人框、英雄卡）
  trackName:     24,  // 行動順序籌碼姓名
  tabLabel:      24,  // 規則面板 Tab 文字
  heroClass:     22,  // 英雄卡職業/元素（戰鬥場景）
  statNum:       22,  // HP/SP 數值、技能名稱
  itemDesc:      22,  // 道具描述

  // ── 極小 ──────────────────────────────────────────────────
  trackInit:     19,  // 行動順序先攻數字
  version:       29,  // 版本號（主選單右下）

  // ── 特殊：游標 ────────────────────────────────────────────
  targetCursor:  34,  // 選敵游標 ▼
  resultSeed:    31,  // 結果畫面種子/難度
  resultStats:   43,  // 結果畫面統計
  mapNodeIcon:   53,  // 地圖節點圖示（一般）
  mapBossIcon:   72,  // 地圖 Boss 節點圖示
};
